export const CONFIG = {
  API_URL: import.meta.env.VITE_API_URL || 'http://localhost:4000',
  // You can add more global settings here later
  WS_URL: import.meta.env.VITE_API_URL || 'http://localhost:4000', 
};